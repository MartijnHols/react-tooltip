import React from 'react';
import Tooltip from 'react-tooltip';

const Content = () => (
  <div>
    This is a piece of text with a tooltip <Tooltip tooltip="Testing test">here</Tooltip>.
  </div>
);

export default Content;
